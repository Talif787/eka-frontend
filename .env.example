# Server-side only: where the Next.js proxy forwards /api/backend/* requests.
# Point this at your running EKA API. With a kubectl port-forward this is
# typically http://localhost:8080; a local uvicorn is http://localhost:8000.
API_BASE_URL=http://localhost:8080
